[user]
	name = Abdallah Hodieb
	email = abdallah.hodieb@hashicorp.com
[core]
	excludesfile = ~/.dotfiles/git/gitignore-global
[pull]
	rebase = true
[rebase]
	autoStash = true
[submodule]
	recurse = true
[init]
        defaultBranch = main
[alias]
	s = status -sb
	r = rebase -i
	r10 = rebase -i HEAD~10
  ll = log --abbrev-commit --decorate --date=relative --format=format:'%C(red)%h%C(r) —— %C(bold blue)%an%C(r): %C(white)%s%C(r) %C(dim white) %C(bold green)(%ar)%C(r) %C(bold yellow)%d%C(r)' --all
	br = branch --format='%(HEAD) %(color:yellow)%(refname:short)%(color:reset) - %(contents:subject) %(color:green)(%(committerdate:relative)) [%(authorname)]' --sort=-committerdate
	commit-all = !git add -A && git commit
	commit-wip = !git add -A && git commit -m '🚧 wip'
	undo = reset HEAD~1 --soft
[url "git@personal.github.com:ahodieb"]
  insteadOf = https://github.com/ahodieb
[url "git@github.com:hashicorp"]
  insteadOf = https://github.com/hashicorp
[commit]
	gpgsign = true
[gpg]
	program = /opt/homebrew/bin/gpg
